@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>NCCI Code List</h2>
<table id="codesTable" class="table table-striped table-hover">
    <thead class="thead-dark">
        <tr>
            <th>Code</th>
            <th>Job Description</th>
        </tr>
    </thead>
    <tbody></tbody>
</table>

@section scripts{
    <script>
        $(document).ready(function () {
            $('#codesTable').DataTable({
                ajax: {
                    url: '/api/codes',
                    dataSrc: ''
                },
                columns: [
                    { data: 'number' },
                    { data: 'jobDescription' }
                    /*{
                        data: 'id',
                        render: function (data) {
                            return '<button class="btn btn-sm btn-outline-info js-delete" data-code-id=' + data + '>Delete</button>';
                        }
                    }*/
                ]
            });
        });
    </script>    
}